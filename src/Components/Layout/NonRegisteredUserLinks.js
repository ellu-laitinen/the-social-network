import React from 'react';
import Login from '../Auth/Login'

const NonRegisteredUserLinks = () => {
    return (
        <div>
            non registered user links
        
        </div>
    );
}

export default NonRegisteredUserLinks;
